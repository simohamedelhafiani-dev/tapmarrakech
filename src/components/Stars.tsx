import { Star } from 'lucide-react';
export function Stars({ rating, size = 16, interactive = false, onSelect }: { rating: number; size?: number; interactive?: boolean; onSelect?: (rating: number) => void }) { return <div className="flex gap-1">{[1,2,3,4,5].map((star) => <button key={star} type={interactive ? 'button' : undefined} disabled={!interactive} onClick={() => onSelect?.(star)} aria-label={`${star} étoile${star > 1 ? 's' : ''}`} className={`${interactive ? 'transition hover:scale-110' : ''}`}><Star size={size} fill={star <= rating ? 'currentColor' : 'none'} className={star <= rating ? 'text-gold' : 'text-ink/15'} /></button>)}</div>; }
